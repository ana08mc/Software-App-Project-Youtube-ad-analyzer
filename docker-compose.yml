version: '3.8'

services:
  youtube-analyzer:
    build: .
    container_name: youtube-ad-analyzer
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
